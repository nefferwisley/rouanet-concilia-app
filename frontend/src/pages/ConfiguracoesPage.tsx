import { useState } from "react";
import { obterRegrasPlataforma, regrasPadrao, salvarRegrasPlataforma } from "../lib/regrasPlataforma";
export function ConfiguracoesPage() {
  const [regras, setRegras] = useState(obterRegrasPlataforma); const [mensagem, setMensagem] = useState<string | null>(null);
  const atualizar = (campo: keyof typeof regras, valor: string | boolean) => setRegras((atual) => ({ ...atual, [campo]: valor }));
  function salvar() { try { new RegExp(regras.formatoIdentificador); } catch { setMensagem("Informe um formato de validação válido."); return; } salvarRegrasPlataforma(regras); setMensagem("Regras salvas. Elas serão usadas nos próximos cadastros de projeto."); }
  return <div className="mx-auto max-w-3xl space-y-6 pb-12"><div><h2 className="text-xl font-bold text-slate-900 dark:text-white">Configurações da plataforma</h2><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Defina como a plataforma identifica os projetos quando a regra não for SALIC/PRONAC.</p></div><section className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm dark:border-navy-700 dark:bg-navy-800"><h3 className="font-semibold text-slate-900 dark:text-white">Identificação do projeto</h3><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Sistema de origem<input className="input mt-1" value={regras.sistemaOrigem} onChange={(e) => atualizar("sistemaOrigem", e.target.value)} placeholder="Ex.: SALIC" /></label><label className="text-sm font-medium">Nome do identificador<input className="input mt-1" value={regras.rotuloIdentificador} onChange={(e) => atualizar("rotuloIdentificador", e.target.value)} placeholder="Ex.: PRONAC" /></label><label className="text-sm font-medium sm:col-span-2">Formato de validação<input className="input mt-1 font-mono" value={regras.formatoIdentificador} onChange={(e) => atualizar("formatoIdentificador", e.target.value)} placeholder="Ex.: ^[A-Z0-9-]{4,20}$" /><span className="mt-1 block text-xs font-normal text-slate-500">Use uma expressão regular. O padrão SALIC aceita de 4 a 8 dígitos.</span></label></div><div className="mt-5 space-y-3 text-sm"><label className="flex items-center gap-2"><input type="checkbox" checked={regras.identificadorObrigatorio} onChange={(e) => atualizar("identificadorObrigatorio", e.target.checked)} /> Exigir identificador no cadastro</label><label className="flex items-center gap-2"><input type="checkbox" checked={regras.consultaSalicAtiva} onChange={(e) => atualizar("consultaSalicAtiva", e.target.checked)} /> Habilitar consulta pública do SALIC</label></div>{mensagem && <p className={`mt-4 text-sm ${mensagem.startsWith("Informe") ? "text-red-600" : "text-emerald-600"}`}>{mensagem}</p>}<div className="mt-6 flex gap-3"><button className="btn-primary" onClick={salvar}>Salvar regras</button><button className="btn-secondary" onClick={() => { setRegras(regrasPadrao); setMensagem(null); }}>Restaurar padrão SALIC</button></div></section></div>;
}