# Protótipos descontinuados

Estas três coisas foram movidas pra cá em 2026-08-09 e **não recebem mais trabalho novo**.
O SaaS real é `backend/` (FastAPI) + `frontend/` (React), deployado em Render+Netlify+Supabase.

- **`api/`** — segunda API FastAPI, duplicada/legada, nunca chegou a ser conectada ao frontend real.
- **`app.py`** — protótipo Streamlit local, feito só pra rodar a conciliação do projeto 1961 offline.
- **`index.html`** — SPA solta em JavaScript puro (Google Sign-In + localStorage), anterior ao SaaS React atual.

Mantidos só como referência histórica. Ver `~/.claude/plans/compiled-doodling-peacock.md` (ou o
histórico de commits) pra contexto de por que cada um foi descontinuado.
